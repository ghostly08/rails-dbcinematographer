<div id="navbar">
  <h3><strong>db</strong>cinematographer</h3>
  <nav>
    <h4>about</h4>
    <h4>contact</h4>
  </nav>

  <% if user_signed_in? %>
    <h3>Hello, <%= current_user.email %> </h3>
    <%= link_to "Log out",  destroy_user_session_path, data: {turbo_method: :delete} %>
  <% end %>

</div>
